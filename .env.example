# Copy to .env and fill in. PowerShell:  Get-Content .env | ForEach-Object { $p = $_ -split '=',2; [Environment]::SetEnvironmentVariable($p[0], $p[1]) }
GCP_PROJECT=your-project-id
BQ_DATASET=elections
BQ_LOCATION=US
CENSUS_API_KEY=your-census-key
