# Copy to .env and fill in, then load with:  . .\env.ps1
# Env vars do not survive a session, so that runs once per terminal.
GCP_PROJECT=your-project-id
BQ_DATASET=elections
BQ_LOCATION=US
CENSUS_API_KEY=your-census-key
